const nico = {
    nickname : "nick"
}

// nico.hello() err

// [1,2,3] + true err

function divide(a, b) {
    return a / b
}

// divide("hell"); err

const player = {
    age : 12
}

// player.age = false err